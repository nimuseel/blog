import styled from '@emotion/styled';

const ProjectList = styled.ul`
  display: flex;
  flex-direction: column;
  gap: 12px;
  list-style: none;
  padding: 0;
  margin: 0;

  p {
    margin: 0;
  }
`;

export default {
  ProjectList,
};
